#include "testlib.h"
// #include "jngen.h"
using namespace std;

int main(int argc, char* argv[]){
    registerGen(argc, argv, 1);
    int N = atoi(argv[1]), M = atoi(argv[2]), Q = atoi(argv[3]);
    bool toMax = atoi(argv[4]);
    int subtask = atoi(argv[5]);
}